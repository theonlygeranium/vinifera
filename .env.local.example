# Optional frontend-only local override.
# `npm run dev` sets this automatically for the Vite process.
VITE_API_BASE_URL=http://127.0.0.1:8788

# The browser calls the Worker, not Supabase directly. Do not add service-role,
# provider, or hosted credentials to this Vite-readable file.
