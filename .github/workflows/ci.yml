name: Quality and staging deployment

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: vinifera-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    name: Type, test, build, and package
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=moderate

      - name: Type-check
        run: npm run typecheck

      - name: Run unit and integration tests
        run: npm test

      - name: Verify Phase 2 through Phase 4 database architecture
        run: |
          npm run qa:db:phase2
          npm run qa:db:phase3
          npm run qa:db:phase4

      - name: Build static assets
        run: npm run build

      - name: Validate Worker bundle
        run: npm run build:worker

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run browser QA
        run: npm run qa:e2e

      - name: Upload QA evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-evidence
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 14

      - name: Validate Pages rollback artifact
        run: |
          npm run build:pages
          test -f dist/app
          grep -q "Fall 2026 Club Release" dist/app

  database:
    name: Apply Supabase migrations when activated
    needs: quality
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Detect migration activation
        id: activation
        shell: bash
        run: |
          if [[ -n "$SUPABASE_ACCESS_TOKEN" && -n "$SUPABASE_DB_PASSWORD" && -n "$SUPABASE_PROJECT_ID" ]]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Supabase migrations are connection-ready but deferred until SUPABASE_ACCESS_TOKEN, SUPABASE_DB_PASSWORD, and SUPABASE_PROJECT_ID are configured."
          fi

      - name: Install Supabase CLI
        if: steps.activation.outputs.ready == 'true'
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link project and apply migrations
        if: steps.activation.outputs.ready == 'true'
        run: |
          supabase link --project-ref "$SUPABASE_PROJECT_ID"
          supabase db push

  deploy-staging:
    name: Deploy Worker when activated
    needs: [quality, database]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      AUTH_EMAIL_ENABLED: ${{ secrets.AUTH_EMAIL_ENABLED }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_WORKERS_DEPLOY_ENABLED: ${{ secrets.CLOUDFLARE_WORKERS_DEPLOY_ENABLED }}
      COMPLIANCE_PROVIDER: ${{ secrets.COMPLIANCE_PROVIDER }}
      COMPLIANCE_SIMULATOR_ENABLED: ${{ secrets.COMPLIANCE_SIMULATOR_ENABLED }}
      EASYPOST_API_KEY: ${{ secrets.EASYPOST_API_KEY }}
      EMAIL_PROVIDER: ${{ secrets.EMAIL_PROVIDER }}
      EMAIL_SIMULATOR_ENABLED: ${{ secrets.EMAIL_SIMULATOR_ENABLED }}
      GOOGLE_OAUTH_ENABLED: ${{ secrets.GOOGLE_OAUTH_ENABLED }}
      RATE_LIMIT_PEPPER: ${{ secrets.RATE_LIMIT_PEPPER }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      RESEND_DOMAIN_VERIFIED: ${{ secrets.RESEND_DOMAIN_VERIFIED }}
      RESEND_FROM: ${{ secrets.RESEND_FROM }}
      RESEND_SENDING_DOMAIN: ${{ secrets.RESEND_SENDING_DOMAIN }}
      RESEND_WEBHOOK_SECRET: ${{ secrets.RESEND_WEBHOOK_SECRET }}
      SHIPPING_PROVIDER: ${{ secrets.SHIPPING_PROVIDER }}
      SHIPPING_SIMULATOR_ENABLED: ${{ secrets.SHIPPING_SIMULATOR_ENABLED }}
      SHIPCOMPLIANT_ACCOUNT_ID: ${{ secrets.SHIPCOMPLIANT_ACCOUNT_ID }}
      SHIPCOMPLIANT_API_KEY: ${{ secrets.SHIPCOMPLIANT_API_KEY }}
      SHIPCOMPLIANT_API_SECRET: ${{ secrets.SHIPCOMPLIANT_API_SECRET }}
      SHIPCOMPLIANT_BASE_URL: ${{ secrets.SHIPCOMPLIANT_BASE_URL }}
      SHIPCOMPLIANT_CHECK_PATH: ${{ secrets.SHIPCOMPLIANT_CHECK_PATH }}
      SHIPCOMPLIANT_CONTRACT_VERSION: ${{ secrets.SHIPCOMPLIANT_CONTRACT_VERSION }}
      SHIPCOMPLIANT_LICENSE_ID: ${{ secrets.SHIPCOMPLIANT_LICENSE_ID }}
      SHIPCOMPLIANT_TOKEN_PATH: ${{ secrets.SHIPCOMPLIANT_TOKEN_PATH }}
      STRIPE_PRICE_CELLAR: ${{ secrets.STRIPE_PRICE_CELLAR }}
      STRIPE_PRICE_ESTATE: ${{ secrets.STRIPE_PRICE_ESTATE }}
      STRIPE_PRICE_RESERVE: ${{ secrets.STRIPE_PRICE_RESERVE }}
      STRIPE_PRICE_VINE: ${{ secrets.STRIPE_PRICE_VINE }}
      STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
      STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      UNSUBSCRIBE_SIGNING_SECRET: ${{ secrets.UNSUBSCRIBE_SIGNING_SECRET }}
    steps:
      - name: Check out source
        uses: actions/checkout@v7

      - name: Detect Worker deployment activation
        id: activation
        shell: bash
        run: |
          if [[ "$CLOUDFLARE_WORKERS_DEPLOY_ENABLED" == "true" && -n "$CLOUDFLARE_ACCOUNT_ID" && -n "$CLOUDFLARE_API_TOKEN" ]]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Worker deployment is build-ready but deferred until CLOUDFLARE_WORKERS_DEPLOY_ENABLED=true and a Workers-capable API token are configured."
          fi

      - name: Use Node.js
        if: steps.activation.outputs.ready == 'true'
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install and build
        if: steps.activation.outputs.ready == 'true'
        run: |
          npm ci
          npm run build

      - name: Deploy staging Worker
        if: steps.activation.outputs.ready == 'true'
        run: npx wrangler deploy

      - name: Apply available Worker secrets
        if: steps.activation.outputs.ready == 'true'
        shell: bash
        run: |
          node --input-type=module <<'NODE' | npx wrangler secret bulk
          const names = [
            "AUTH_EMAIL_ENABLED",
            "COMPLIANCE_PROVIDER",
            "COMPLIANCE_SIMULATOR_ENABLED",
            "EASYPOST_API_KEY",
            "EMAIL_PROVIDER",
            "EMAIL_SIMULATOR_ENABLED",
            "GOOGLE_OAUTH_ENABLED",
            "RATE_LIMIT_PEPPER",
            "RESEND_API_KEY",
            "RESEND_DOMAIN_VERIFIED",
            "RESEND_FROM",
            "RESEND_SENDING_DOMAIN",
            "RESEND_WEBHOOK_SECRET",
            "SHIPPING_PROVIDER",
            "SHIPPING_SIMULATOR_ENABLED",
            "SHIPCOMPLIANT_ACCOUNT_ID",
            "SHIPCOMPLIANT_API_KEY",
            "SHIPCOMPLIANT_API_SECRET",
            "SHIPCOMPLIANT_BASE_URL",
            "SHIPCOMPLIANT_CHECK_PATH",
            "SHIPCOMPLIANT_CONTRACT_VERSION",
            "SHIPCOMPLIANT_LICENSE_ID",
            "SHIPCOMPLIANT_TOKEN_PATH",
            "STRIPE_PRICE_CELLAR",
            "STRIPE_PRICE_ESTATE",
            "STRIPE_PRICE_RESERVE",
            "STRIPE_PRICE_VINE",
            "STRIPE_PUBLISHABLE_KEY",
            "STRIPE_SECRET_KEY",
            "STRIPE_WEBHOOK_SECRET",
            "SUPABASE_ANON_KEY",
            "SUPABASE_PUBLISHABLE_KEY",
            "SUPABASE_SECRET_KEY",
            "SUPABASE_SERVICE_ROLE_KEY",
            "SUPABASE_URL",
            "UNSUBSCRIBE_SIGNING_SECRET"
          ];
          const values = Object.fromEntries(
            names
              .filter((name) => process.env[name])
              .map((name) => [name, process.env[name]])
          );
          process.stdout.write(JSON.stringify(values));
          NODE
